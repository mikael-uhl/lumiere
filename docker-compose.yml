version: '3'
volumes:
  dados:
services:
  db:
    container_name: db
    image: postgres:9.6
    volumes:
      - dados:/var/lib/postgres/data
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
    ports:
      - "5432:5432"
